name: CI

on:
  push:
    branches:
      - 'main'
      - 'release-v**'
    tags:
      - 'v[0-9]*'
  pull_request:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Set up JDK 11
        uses: actions/setup-java@v1
        with:
          java-version: 11

